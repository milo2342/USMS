USMS bot updated.


## Command role permissions
Use `/permissions add` to grant an existing Discord role access to a specific department command, `/permissions remove` to revoke it, `/permissions view` to list permissions, and `/permissions clear` to return a command to owner-only access. Bot owners always retain access.
